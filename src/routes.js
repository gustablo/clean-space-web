import React from 'react';

import { Switch, Route } from 'react-router-dom';

export default function RoutesModule() {
    return (
        <Switch>
            <Route path="/fds" component={null} />
        </Switch>
    );
}
